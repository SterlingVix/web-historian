// The second application will read the list of URLs from your file and go get the URLs from the internet, saving them onto your computer.
  // You'll run this one on a regular basis, using cron.

////////////////////////////////////////////

// eventually, you'll have some code here that uses the code in `archive-helpers.js`
// to actually download the urls you want to download.
  var path = require('path');
  
// Functions here will be available through   var htmlFetcher = require('../workers/htmlfetcher');
module.exports = function() { // this ought to export all functions inside.
  
  
  
  
}; // end module.exports